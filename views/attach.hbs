<h1>Attach a new accessory</h1>
<div class="form">
    <h2>{{car.name}}</h2>
    <img class="cube" src="/static/images/{{car.imageUrl}}">
    {{#if accessories}}
    <form action="/attach/{{car.id}}" method="POST">
        <label for="accessory">Accessories</label>
        <select id="accessory" name="accessory" />
        {{#each accessories}}
        <option value="Accessory 1">Accessory 1</option>
        <option value="Accessory 2">Accessory 2</option>
        <option value="Accessory 3">Accessory 3</option>
        <option value="Accessory 4">Accessory 4</option>
        <option value="Accessory 5">Accessory 5</option>
        {{/each}}
        </select>
        <input type="submit" value="Attach">
    </form>
    {{else}}
    <h3 class="italic">This car has all available accessories so far or there is no available at all...
    </h3>
    {{/if}}
    <a class="btn" href="/details/{{car.id}}">Back</a>
</div>